#include <stdio.h>

int main() { 
	fwrite()
	printf("stdin->fileno = %d\n", stdin->_fileno);
	printf("stdout->fileno = %d\n", stdout->_fileno);
	printf("stderr->fileno = %d\n", stderr->_fileno);
	return 0;
} 
